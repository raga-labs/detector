[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "raga-detector"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
raga-detect = "raga_detector.cli:main"

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[tool.setuptools.packages.find]
where = ["src"]
